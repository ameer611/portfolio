node_modules
dist
.DS_Store
*.local

# Local agent/editor settings (machine-specific)
.claude/settings.local.json

# Source image pasted in while working — not part of the site
Pasted image.png
image.png

# Fonts cached by tools/og-image.py
tools/.fonts/
