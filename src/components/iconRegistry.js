import { GithubIcon, LinkedinIcon, MediumIcon } from './icons'

// Kept out of icons.jsx so that file exports only components and React
// Fast Refresh keeps working.
export const socialIcons = {
  github: GithubIcon,
  linkedin: LinkedinIcon,
  medium: MediumIcon,
}
